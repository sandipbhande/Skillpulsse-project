apiVersion: v1
kind: Service
metadata:
  name: skillpulse-backend
  labels:
    app: skillpulse
    component: backend
spec:
  type: ClusterIP
  selector:
    app: skillpulse
    component: backend
  ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
      name: http
